<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ecobrandlyapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EcoBrandlyApp">
        <activity
            android:name=".CreateQrActivity"
            android:exported="true" />
        <activity
            android:name=".HomeEnterpriseActivity"
            android:exported="true" />
        <activity
            android:name=".RegisterEnterpriseActivity"
            android:exported="true" />
        <activity
            android:name=".RegistrationOptionActivity"
            android:exported="true" />
        <activity
            android:name=".HomeActivity"
            android:exported="true" />
        <activity
            android:name=".RegisterActivity"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter> <!-- 처음실행되도록 한다 -->
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CafeQRScanActivity" />
    </application>

</manifest>